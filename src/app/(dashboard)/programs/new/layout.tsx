// La création lit la page du programme (téléchargement + analyse) : plus long qu'un simple insert.
export const maxDuration = 60;

export default function NewProgramLayout({ children }: { children: React.ReactNode }) {
  return children;
}
